-- \i '/docker-entrypoint-initdb.d/tables/banks.sql'

-- \i '/docker-entrypoint-initdb.d/tables/categories.sql'

-- \i '/docker-entrypoint-initdb.d/tables/transactions.sql'

-- \i '/docker-entrypoint-initdb.d/seed/seed_banks.sql'

-- \i '/docker-entrypoint-initdb.d/seed/seed_transactions.sql'

-- \i '/docker-entrypoint-initdb.d/seed/seed_categories.sql'